export type DepositNote = {
    system: string;
    date: string;
};
export declare class SolanaUtils {
    static mnemonicToSecretKey(mnemonic: string): Promise<Uint8Array>;
    static generateMnemonic(): string;
}
