import { SolanaAccount } from '../accounts';
import { Connection, PublicKey, Transaction } from "@solana/web3.js";
import { BridgeToken, Routing } from "../../../common";
import { SolanaAccountsConfig } from "../config";
export declare class SolanaBridgeTxnsV1 {
    private _bridgeProgramAddress;
    private _primarySeed;
    private _client?;
    private _accounts;
    constructor(client: Connection, bridgeProgramAddress: string, accounts: SolanaAccountsConfig);
    getSolEscrowAccount(account: PublicKey): Promise<PublicKey>;
    getTokenEscrowAccount(account: PublicKey): Promise<PublicKey>;
    getAssetInfoAccount(account: PublicKey, mintAccount: PublicKey): Promise<PublicKey>;
    getTokenAccount(account: PublicKey, mintAccount: PublicKey): Promise<PublicKey>;
    HandleUsdcSwap(account: SolanaAccount, routing: Routing): Promise<Transaction | undefined>;
    solBridgeTransaction(account: PublicKey, routing: Routing, token: BridgeToken): Promise<Transaction | undefined>;
    tokenBridgeTransaction(account: PublicKey, routing: Routing, token: BridgeToken): Promise<Transaction | undefined>;
}
