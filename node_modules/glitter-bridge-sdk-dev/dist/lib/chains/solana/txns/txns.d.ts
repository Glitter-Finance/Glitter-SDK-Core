import { Connection, PublicKey, Transaction } from "@solana/web3.js";
import { BridgeToken, Routing } from "../../../common";
export declare class SolanaTxns {
    private _client?;
    private _solToken;
    constructor(client: Connection);
    get SolToken(): BridgeToken | undefined;
    sendSolTransaction(routing: Routing): Transaction;
    sendTokenTransaction(routing: Routing, senderTokenAccount: PublicKey, recipientTokenAccount: PublicKey, token: BridgeToken): Transaction;
    closeTokenAccountTransaction(senderAccount: PublicKey, senderTokenAccount: PublicKey): void;
}
