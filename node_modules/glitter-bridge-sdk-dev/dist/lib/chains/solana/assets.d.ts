import { Account } from "@solana/spl-token";
import { Connection, PublicKey, Signer } from "@solana/web3.js";
import { BridgeToken } from "../../common";
export type SolanaAsset = {
    name: string;
};
export declare class SolanaAssets {
    private _assets;
    private _client?;
    constructor(client: Connection);
    getTokenAddress(mint: PublicKey, owner: PublicKey): Promise<PublicKey>;
    createTokenAccount(signer: Signer, owner: PublicKey, token: BridgeToken): Promise<Account>;
    getTokenAccount(owner: PublicKey, token: BridgeToken): Promise<Account | undefined>;
}
