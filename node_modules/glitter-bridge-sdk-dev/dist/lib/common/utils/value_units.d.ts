export declare class ValueUnits {
    value: number;
    units: bigint;
    static fromValue(value: number, decimals: number): ValueUnits;
    static fromUnits(units: bigint, decimals: number): ValueUnits;
}
